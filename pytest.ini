[pytest]
testpaths = tests
markers =
  signed_chain: tests that require cryptographically signed events and valid prev_hash chain
  unsigned_events: tests that use non-cryptographic event fixtures (fake signatures/prev_hash)
